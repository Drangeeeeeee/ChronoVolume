# ChronoVolume 功能与优化路线

## 1. 素材导入

### 现有功能

- 支持导入多个视频素材。
- 识别分辨率、帧数、FPS、bit depth、Alpha 状态。
- 可切换当前显示素材。
- 支持素材缺失检查。
- 支持代理缓存、高精度 raw cache。

### 优化路线

- P0：素材状态统一管理，避免某些视图重新读取同一视频。
- P1：完善“脱机素材查找/重连”。
- P1：素材导入时记录色彩空间、gamma、HDR 信息。
- P2：支持音频读取，为表达式和频谱驱动做准备。

## 2. 2D / 3D 时间体视图

### 现有功能

- 2D 播放与时间轴查看。
- 3D 时间体视图。
- Alpha 体 / 像素体渲染。
- 背景颜色、透明背景、棋盘格。
- 摄像机示意、世界坐标轴、参考面显示。
- 图层 / Alpha 体可调整位置、旋转、缩放。

### 优化路线

- P0：继续统一世界视图和原 3D 视图的交互逻辑。
- P1：优化 Gizmo 命中，避免拖视角时误拖 XYZ 轴。
- P1：提高大体积场景预览流畅度。
- P2：加入视图书签、正交 / 透视切换、对齐到轴向视角。

## 3. 摄像机系统

### 现有功能

- 3D 摄像机位置、方向、焦段、光圈。
- 摄像机视图、世界视图、所有摄像机视图。
- 摄像机可作为时间线层存在。
- 支持多个摄像机、隐藏摄像机、分割摄像机。
- 支持焦点锁定。
- 可导出摄像机画面。

### 优化路线

- P0：确保所有摄像机视图中每个摄像机窗口完全独立、可拖拽、可调整大小。
- P1：摄像机运动路径可视化。
- P1：摄像机视锥显示。
- P2：景深、运动模糊、焦点跟随目标图层。

## 4. 合成工作区

### 现有功能

- 可新建多个合成。
- 合成可设置尺寸、帧数、FPS、背景。
- 多图层时间线。
- 图层可拖拽排序。
- 图层可拖动、拉伸、分割、删除。
- 支持预合成。
- 预合成可像普通合成一样打开。
- 支持合成导出设置窗口。

### 优化路线

- P0：继续整理合成工作区 UI 密度。
- P0：时间线 / 属性区 / 预览区布局保存。
- P1：预合成标签页化，类似 AE 的合成标签。
- P1：图层搜索、筛选可见 / 锁定 / 有关键帧。
- P2：父子层级、空对象、对齐工具。

## 5. 时间线与关键帧

### 现有功能

- 图层和摄像机属性关键帧。
- 关键帧框选、移动、删除。
- Shift 吸附关键帧。
- 支持线性、缓入缓出、保持、贝塞尔。
- 曲线视图 / 关键帧视图。
- 支持复制粘贴图层、摄像机、关键帧。
- 支持撤销 / 重做。

### 优化路线

- P0：继续打磨时间线缩放和滚轮行为。
- P1：曲线编辑器专业化：缩放、平移、多属性归一化、速度显示。
- P1：复制 / 粘贴 easing。
- P2：关键帧辅助工具，例如反转、平滑、循环、偏移。

## 6. 表达式系统

### 现有功能

- 属性旁可点 `ƒ` 添加表达式。
- 支持 `frame`、`time`、`fps`、`value`。
- 支持 `sin`、`cos`、`noise`、`clamp`、`lerp`、`min`、`max`、`pow` 等函数。
- 支持绑定图层或摄像机属性。
- 表达式随项目保存。

### 优化路线

- P0：增加表达式错误提示和高亮。
- P1：加入表达式预览值。
- P1：真实音频频谱驱动。
- P2：插件系统：自定义函数、表达式预设、脚本扩展。

## 7. 混合模式与遮罩

### 现有功能

- 正常、相加、屏幕、正片叠底。
- 轨道遮罩 / Alpha 遮罩。
- 图层可见、锁定、Solo。

### 优化路线

- P0：继续修正轨道遮罩的空间 / Alpha 采样逻辑。
- P1：加入 Luma Matte、Invert Matte。
- P1：遮罩预览模式。
- P2：真正的 3D 布尔 / 体积遮罩。

## 8. 导出系统

### 现有功能

- 本机导出。
- 分布式导出。
- 摄像机导出。
- 合成导出。
- 可设置 bit depth、Alpha、背景、分辨率、FPS、导出范围。
- 支持导出诊断日志。

### 优化路线

- P0：导出失败时不崩溃，统一错误提示。
- P1：渲染队列：多个导出任务排队、暂停、继续、重试。
- P1：导出日志持久化。
- P2：导出预设，例如 ProRes 4444、H.264、PNG 序列、EXR。

## 9. 分布式渲染

### 现有功能

- 支持多台 Mac Worker。
- 支持手动分配、自动分配、动态 chunk。
- 支持 Worker 源文件发送、raw cache 预热。
- 支持诊断数据记录。

### 优化路线

- P0：稳定多 Worker 调度状态显示。
- P1：根据历史 FPS 自动更新机器权重。
- P1：Worker 缓存状态统一管理。
- P2：局域网 Worker 自动发现。

## 10. 项目系统

### 现有功能

- 项目保存 / 打开。
- `.CV` 项目格式。
- 保存素材、合成、图层、关键帧、摄像机、背景、导出设置。
- 支持自动保存 / 崩溃恢复。
- 支持未保存关闭提醒。
- 支持新建项目 / 关闭项目。

### 优化路线

- P0：项目版本号与迁移机制继续强化。
- P1：项目打包功能，把素材和缓存一并收集。
- P2：项目模板、最近项目、自动备份历史版本。

## 11. 缓存与诊断

### 现有功能

- raw cache。
- 代理缓存。
- 高精度缓存。
- 缓存清理。
- 媒体管理器。
- 性能 / 诊断面板。
- 错误报告 / 诊断日志包。

### 优化路线

- P0：统一缓存策略中心。
- P1：显示缓存是否过期、占用大小、命中率。
- P1：按项目 / 素材 / 合成清理。
- P2：后台预构建代理和高精度缓存。

## 12. 色彩管理

### 现有功能

- 已有 bit depth 选择。
- 已开始关注 Alpha 边缘发黑、色彩断层问题。

### 优化路线

- P0：识别源视频色彩空间和 gamma。
- P1：导出 Rec.709 / Display P3 / HDR。
- P1：预乘 Alpha / straight Alpha 明确切换。
- P2：32-bit float 工作流和 EXR 输出。

## 建议优先级

1. 稳定性：导出不崩、项目不丢、时间线不抽。
2. 合成工作区手感：时间线、曲线、图层交互继续打磨。
3. 缓存策略中心：让复杂工程不卡。
4. 色彩管理：避免偏色、断层、Alpha 黑边。
5. 表达式 + 音频驱动：让 ChronoVolume 更接近真正的动态图形工具。
