# ChronoVolume 已有功能与已完成优化记录

本文档用于记录 ChronoVolume 目前已经具备的功能，以及开发过程中已经完成过的主要优化、修复和结构升级。

## 1. 项目与工程结构

### 已有功能

- 程序已更名为 ChronoVolume。
- 工程结构已完成项目级重命名。
- 支持 `.CV` 项目文件。
- 支持项目保存、打开、另存为。
- 支持快捷键保存：
  - `Command + S`：保存项目。
  - `Command + Shift + S`：另存为。
- 支持新建项目、关闭项目。
- 支持未保存关闭提醒。
- 支持自动保存和崩溃恢复。
- 项目文件可保存：
  - 导入素材列表。
  - 合成。
  - 图层。
  - 关键帧。
  - 摄像机。
  - 背景。
  - 导出设置。

### 已完成优化

- 从早期单视频工作流扩展为多素材、多合成项目工作流。
- 增加项目状态保存，避免复杂工程丢失。
- 增加自动保存和恢复入口，降低崩溃损失。
- 将项目文件后缀改为 `.CV`。

## 2. 素材导入与媒体管理

### 已有功能

- 支持导入单个或多个视频素材。
- 主按钮仍显示为“导入视频”，但导入逻辑已支持多选。
- 支持在 2D / 3D / 合成视图中选择当前显示素材。
- 支持识别素材信息：
  - 分辨率。
  - 帧数。
  - FPS。
  - bit depth。
  - Alpha 状态。
- 支持素材缺失检查。
- 支持脱机素材查找。
- 支持媒体管理器，显示：
  - 素材是否丢失。
  - 缓存大小。
  - bit depth。
  - Alpha 状态。
  - raw cache 状态。
- 支持一键清理缓存。

### 已完成优化

- 修复连续导入视频时旧任务可能覆盖新视频的问题。
- 增加导入任务取消/校验机制，避免慢任务回写旧素材状态。
- 多素材导入后，界面可选择具体显示哪个素材。
- 素材列表移入左侧项目区，使合成工作区更接近真实项目管理逻辑。
- 增加素材丢失检查和脱机查找流程。

## 3. 视频体数据与缓存

### 已有功能

- 支持从视频构建时间体数据。
- 支持 3D 预览体和高精度工作体。
- 支持 raw cache。
- 支持代理缓存。
- 支持高精度缓存。
- 支持缓存刷新、清理、预构建。
- 支持缓存策略中心。
- 支持缓存是否过期、缓存状态、缓存清理入口。

### 已完成优化

- 修复 `previewMaxDepth` 被忽略的问题，使预览体和完整体不再被强制统一。
- 修复主解码路径未应用 `preferredTransform` 的问题，避免竖屏视频方向错误。
- 为 Worker 增加源文件发送、raw cache 预热、导入视频会话机制。
- 增加代理预览 / 预渲染缓存，减轻合成编辑压力。
- 增加缓存策略中心，统一管理 raw cache、代理缓存和高精度缓存。

## 4. 2D 播放与基础视图

### 已有功能

- 支持 2D 播放。
- 支持时间轴切换和播放控制。
- 支持导入后查看当前素材状态。
- 支持主界面显示当前素材、Alpha、分辨率、帧数等信息。

### 已完成优化

- 修复播放观察者释放问题。
- `AppModel` 释放时会清理 AVPlayer 观察者、displayLink / timer。
- 修复在部分输入框聚焦时空格误触播放或输入的问题。
- 播放中拖动时间线时会自动处理播放状态，减少状态错乱。

## 5. 3D 时间体视图

### 已有功能

- 支持 3D 时间体视图。
- 支持 Alpha 体渲染。
- 支持像素体渲染。
- 支持边缘连续功能。
- 支持背景颜色修改。
- 支持棋盘格背景。
- 支持透明背景。
- 支持世界坐标轴显示。
- 支持参考面显示。
- 支持摄像机图标和方向示意。
- 支持体属性：
  - 位置。
  - XYZ 旋转。
  - 缩放。

### 已完成优化

- 增加 3D 背景自定义颜色和棋盘格。
- 修复 Alpha 区域背景仍显示黑色的问题。
- 增加 Alpha 边缘连续处理，减轻透明边缘发黑。
- 为像素体渲染增加边缘连续选项。
- 修复摄像机、参考面、原点坐标轴和体变换之间的坐标映射问题。
- 修复体视图旋转时摄像机示意位置不同步的问题。
- 修复体缩放时摄像机与 Alpha 体相对位置显示错误的问题。
- 将原点坐标轴作为世界锚点，不再跟随 Alpha 体变换。

## 6. 参考面切片与高精度导出

### 已有功能

- 支持参考面切片。
- 支持参考面在 3D 视图中显示。
- 支持参考面高精度导出。
- 支持参考面导出接入分布式导出。
- 支持参考面导出使用 raw cache。
- 支持参考面 GPU 路径和 CPU fallback。

### 已完成优化

- 将参考面切片接入分布式导出。
- 增加参考面高精度导出。
- 优化参考面导出速度。
- 修复参考面导出中 GPU 纹理创建失败时的 fallback。
- 修复参考面与体变换速度不一致的问题。
- 修复参考面位移只有体位移一部分的问题。
- 修复参考面显示位置与体位置不一致的问题。

## 7. 摄像机系统

### 已有功能

- 支持 3D 摄像机。
- 支持摄像机属性：
  - 位置 X / Y / Z。
  - Yaw 偏航。
  - Pitch 俯仰。
  - Roll 翻滚。
  - 焦段。
  - 光圈。
  - 焦点锁定。
  - 焦点坐标。
- 支持摄像机视图。
- 支持世界视图。
- 支持所有摄像机视图。
- 支持摄像机视图独立悬浮窗口。
- 支持多个摄像机。
- 摄像机可作为时间线层存在。
- 支持摄像机隐藏。
- 支持摄像机分割。
- 支持摄像机导出。

### 已完成优化

- 增加摄像机时间线。
- 增加摄像机关键帧。
- 增加摄像机焦点锁定功能。
- 修复焦点锁定时 YPR 数值不同步的问题。
- 修复 YPR 修改时摄像机位置错误变化的问题。
- 修复悬浮摄像机窗口和原视图状态不同步的问题。
- 修复悬浮摄像机窗口关闭后原摄像机视图不响应的问题。
- 修复悬浮窗口前台层级问题。
- 修复摄像机视图中拖动视角后撤销记录错误的问题。
- 增加复位摄像机和复位摄像机各属性功能。
- 在世界视图中显示摄像机示意和方向。

## 8. 合成工作区

### 已有功能

- 支持新建合成。
- 支持多个合成。
- 支持打开不同合成。
- 支持预合成。
- 支持将多个图层打包成一个合成层。
- 支持像普通合成一样打开预合成。
- 支持将合成作为素材加入其他合成。
- 支持合成设置：
  - 宽度。
  - 高度。
  - 帧数。
  - FPS。
  - 背景颜色。
  - 透明背景。
- 支持合成视图：
  - 摄像机视图。
  - 世界视图。
  - 所有摄像机视图。
- 支持合成导出。

### 已完成优化

- 增加合成工作区。
- 将素材拖入时间线形成图层。
- 合成视图套用 3D 视图的体 / 场景 / 摄像机逻辑，并适配多个体共存。
- 修复只最后加入的图层属性生效的问题。
- 修复多个图层被同一个属性变换一起影响的问题。
- 增加每个图层独立属性控制。
- 增加合成设置窗口，使用 `Command + K` 打开。
- 隐藏部分重复设置，减少主界面拥挤。
- 增加合成导出设置窗口。
- 合成默认背景改为透明。

## 9. 图层系统

### 已有功能

- 支持视频图层。
- 支持摄像机图层。
- 支持预合成图层。
- 支持图层：
  - 位置。
  - 旋转。
  - 缩放。
  - 不透明度。
  - 开始时间。
  - 时长。
  - 可见 / 隐藏。
  - 锁定。
  - Solo。
- 支持图层拖拽排序。
- 支持图层拖动时间位置。
- 支持左右拉伸改变时长。
- 支持图层分割。
- 支持选中图层按 Delete 删除。
- 支持 `Command / Control + Shift + D` 分割图层。
- 支持 Command 多选。
- 支持 Shift 连选。

### 已完成优化

- 增加图层小眼睛按钮。
- 增加锁定和 Solo。
- 去掉多余删除按钮，保留键盘删除。
- 增加图层拖拽排序。
- 优化图层拖拽灵敏度，避免轻微移动直接跳层。
- 修复展开图层属性后覆盖下一个图层的问题。
- 修复图层时长改变导致范围外关键帧被删除的问题。
- 支持图层两端超出合成时间线，并通过中键切换显示时间线外部分。

## 10. 时间线与关键帧

### 已有功能

- 支持图层属性关键帧。
- 支持摄像机属性关键帧。
- 每个属性左侧有关键帧菱形按钮。
- 支持自动关键帧：某属性已有关键帧时，在其他时间修改会自动打关键帧。
- 支持框选关键帧。
- 支持移动关键帧。
- 支持删除关键帧。
- 支持复制 / 粘贴关键帧。
- 支持关键帧插值：
  - 线性。
  - 缓入缓出。
  - 保持。
  - 贝塞尔。
- 支持曲线视图。
- 支持只显示选中关键帧曲线。
- 支持时间线缩放。
- 支持时间线平移。
- 支持缩放概览条。
- 支持吸附开关。
- 支持时间线标记。

### 已完成优化

- 将关键帧设置改为属性左侧菱形按钮。
- 增加曲线编辑器。
- 增加贝塞尔曲线手柄。
- 修复贝塞尔曲线无法拖动的问题。
- 修复曲线界面拖手柄时误触框选的问题。
- 修复时间线点击位置后移约 100 帧的问题。
- 修复关键帧位置和播放头实线不一致的问题。
- 修复虚线时间码错位问题，并保留全局实线。
- 优化时间线缩放焦点，使其围绕鼠标位置缩放。
- 修复时间线缩放时左侧属性区被一起缩放的问题。
- 修复 Option + 滚轮缩放串到上下滚动的问题。
- 增加最大缩放限制。
- 修复缩放时图层、属性、红线闪烁问题。
- 增加时间线缩放比例和位置概览条。

## 11. 属性输入与角度显示

### 已有功能

- 属性右侧数值可直接输入。
- 属性支持按住名称左右拖动调整数值。
- 每个属性支持复位。
- 旋转角度支持 `AxB°` 显示：
  - `A` 为圈数。
  - `B` 为当前圈内角度。

### 已完成优化

- 将角度显示从单纯度数升级为 `AxB°`。
- 修复滑块进位时重复进位的问题。
- 修复滑到端点松手后从 `1x0°` 变成 `2x0°` 的问题。
- 修复 `5x0°` 和 `-5x0°` 时滑块不会自动回中的问题。
- 修复输入框未确认时误触空格播放或其他属性拖动的问题。
- 修复点击输入框但未修改也自动创建关键帧的问题。
- 修复点击空白区域焦点仍停留在输入框的问题。

## 12. 表达式系统

### 已有功能

- 属性旁可点 `ƒ` 添加表达式。
- 表达式随项目保存。
- 支持变量：
  - `frame`
  - `time`
  - `fps`
  - `value`
- 支持函数：
  - `sin`
  - `cos`
  - `tan`
  - `noise`
  - `clamp`
  - `lerp`
  - `min`
  - `max`
  - `pow`
  - `sqrt`
  - `abs`
- 支持绑定图层属性：
  - `layer("图层名", "positionX")`
- 支持绑定摄像机属性：
  - `camera("yaw")`
  - `camera("摄像机 1", "focalLength")`
- 旋转类表达式按度输入，内部自动转为弧度。
- 音频相关表达式函数已预留占位：
  - `audio(time)`
  - `spectrum(time)`

### 已完成优化

- 从早期函数驱动需求升级为属性表达式系统。
- 将表达式接入图层和摄像机属性求值链路。
- 表达式可叠加在关键帧之上。
- 增加循环引用保护，避免属性互绑导致死循环。
- 增加表达式 UI 入口。

## 13. 混合模式与遮罩

### 已有功能

- 支持图层混合模式：
  - 正常。
  - 相加。
  - 屏幕。
  - 正片叠底。
  - 遮罩 / 轨道遮罩。
- 支持 Alpha 轨道遮罩。

### 已完成优化

- 增加图层混合模式。
- 增加轨道遮罩。
- 修复轨道遮罩识别视频体外部 Alpha 区域的问题。
- 修复轨道遮罩对原 Alpha 通道识别错误的问题。
- 增加图层拖拽层级调整，方便控制遮罩与被遮罩图层顺序。

## 14. 导出系统

### 已有功能

- 支持本机导出。
- 支持分布式导出。
- 支持参考面导出。
- 支持摄像机导出。
- 支持合成导出。
- 支持导出设置：
  - 分辨率。
  - FPS。
  - bit depth。
  - Alpha。
  - 背景。
  - 是否使用代理。
  - 是否高精度。
  - 导出范围。
- 支持 8-bit / 16-bit / 32-bit 选择。
- 支持导出诊断数据。

### 已完成优化

- 修复保存面板取消后卡在导出界面的问题。
- 增加导出 Alpha 独立开关。
- 增加导出 bit depth 选择，并默认跟随源视频 bit depth。
- 优化 Alpha 边缘黑边与色彩断层问题。
- 增加高精度导出路径。
- 增加合成导出设置窗口。
- 修复合成导出 AVAssetWriterInput 参数导致崩溃的问题。
- 增加错误报告 / 诊断日志包。

## 15. 分布式导出

### 已有功能

- 支持多台 Mac Worker。
- 支持 Worker 列表。
- 支持测试全部 Worker。
- 支持发送当前源文件到 Worker。
- 支持 Worker 导入视频会话。
- 支持 Worker raw cache 预热。
- 支持刷新源文件和缓存检查。
- 支持多种调度方式：
  - 动态 chunk。
  - 自动分配。
  - 手动分配。
- 支持多 Worker 手动分配滑块。
- 支持分布式导出诊断记录。

### 已完成优化

- 修复 100% 本机分布式导出时仍创建无效 Worker 任务的问题。
- 修复 Worker 0 帧区间显示为反向范围的问题。
- 将单 Worker 分布式扩展为任意台 Worker。
- 增加 Worker 源文件检查与 raw cache 状态显示。
- Worker 状态移动到对应 Worker 列表项下方显示。
- 增加 Worker 预热 raw cache 和导入视频会话。
- 增加动态 chunk / 自动分配 / 手动分配选择弹窗。
- 手动分配支持多 Worker 多滑块。
- 手动分配滑块支持右侧百分比输入。
- 修复手动输入百分比按回车会直接开始导出的问题。
- 增加本机和 Worker 耗时诊断记录。
- 分布式导出完成后保存诊断 JSON。
- 优化 Worker 贡献过低问题，改进任务分配和缓存策略。

## 16. 渲染队列与诊断

### 已有功能

- 支持渲染队列。
- 可添加多个合成导出任务。
- 每个任务可独立设置：
  - 分辨率。
  - bit depth。
  - Alpha。
  - 导出范围。
- 支持暂停、继续、失败重试。
- 导出完成后保留日志和诊断数据。
- 支持性能 / 诊断面板。
- 支持显示：
  - 预览 FPS。
  - GPU / CPU 路径。
  - 缓存命中率。
  - 显存 / 内存压力。

### 已完成优化

- 增加渲染队列。
- 增加导出任务诊断记录。
- 增加性能 / 诊断面板。
- 增加错误报告 / 诊断日志包，一键汇总项目、素材、缓存、导出设置和最近错误。

## 17. 预览质量

### 已有功能

- 合成视图支持预览质量档位：
  - 草稿。
  - 半分辨率。
  - 全分辨率。
  - 高精度。
  - 自动。
- 编辑时可用低质量预览减轻压力。
- 导出时仍可走高精度路径。

### 已完成优化

- 增加代理预览 / 预渲染缓存。
- 增加预览质量档位。
- 将预览质量接入合成渲染器。
- 让编辑阶段更轻，最终导出保留高精度。

## 18. UI 与交互整理

### 已有功能

- 左侧项目区。
- 中间 / 右侧预览区。
- 下方时间线。
- 属性区与时间线合并显示。
- 支持布局保存。
- 支持属性搜索。
- 支持属性折叠预设。
- 支持时间线搜索和过滤。

### 已完成优化

- 移除部分重复主界面内容。
- 将 2D / 3D / 合成切换逻辑整理到左侧工作区。
- 将项目管理移到左侧。
- 隐藏合成设置，改为快捷键打开。
- 优化时间线、属性区、预览区大小比例。
- 支持像窗口一样调整功能区大小。
- 增加属性搜索 / 折叠预设。
- 增加视图内变换 Gizmo。

## 19. 色彩与 Alpha

### 已有功能

- 支持识别 bit depth。
- 支持导出 bit depth 选择。
- 支持 Alpha 导入和导出。
- 支持透明背景。
- 支持棋盘格显示透明区域。
- 支持色彩管理基础功能。

### 已完成优化

- 修复 Alpha 边缘发黑问题。
- 修复部分色彩断层问题。
- 增加 bit depth 选择。
- 将导出默认 bit depth 改为跟随源视频。
- 增加色彩管理模块，开始识别源视频色彩空间 / gamma / HDR 信息。
- 增加导出 Rec.709、Display P3、HDR 的方向性设置。

## 20. 目前程序定位

ChronoVolume 目前已经从早期的视频切片工具，升级为一个围绕“时间体”的 3D 合成与渲染工具。它已经具备以下核心能力：

- 多素材项目管理。
- 时间体构建和 3D 查看。
- 多图层合成。
- 摄像机动画。
- 关键帧和曲线编辑。
- 表达式驱动。
- 预合成。
- 缓存与代理预览。
- 本机与分布式导出。
- 渲染队列。
- 诊断与错误报告。

整体方向已经接近一个面向时间体视频的轻量动态图形 / 3D 合成软件。
