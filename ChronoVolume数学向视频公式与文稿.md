# ChronoVolume 数学向视频公式与文稿

## 视频定位

建议标题：

**《把视频变成三维时间体：ChronoVolume 背后的数学》**

建议时长：4 到 6 分钟。

风格：偏数学、偏可视化，但不做成纯公式课。画面以程序实录为主，公式作为叠字出现，旁白解释“公式在程序里做了什么”。

核心叙事：

普通视频是一串二维图像；ChronoVolume 把它写成三维函数 `V(x,y,t)`。当时间变成空间维度后，我们可以旋转、切片、渲染、合成，甚至把复杂视频变成可计算、可观察、可导出的几何对象。

## 需要重点出现的公式

### 1. 视频作为时空体

$$
V(x,y,t)=(R,G,B,A)
$$

含义：

`x,y` 是画面中的空间坐标，`t` 是时间或帧序号，`R,G,B,A` 是该位置在该时刻的颜色与透明度。

旁白解释：

一段视频可以看成一个三维函数。每一帧是这个函数在某一个 `t` 上的截面；把所有帧沿时间方向堆起来，就得到一个视频体。

### 2. 体数据存储索引

$$
\text{index}(x,y,t)=((tH+y)W+x)\times4
$$

含义：

三维体数据在内存里仍然是一维数组。每个体素有 RGBA 四个通道，所以最后乘以 4。

### 3. 视频尺寸与时间深度缩放

$$
s=\min\left(1,\frac{W_{\max}}{W},\frac{H_{\max}}{H}\right)
$$

$$
W'=\mathrm{round}(Ws),\quad H'=\mathrm{round}(Hs)
$$

$$
D'=\min\left(F,\mathrm{round}(F\cdot s)\right)
$$

含义：

为了让预览足够流畅，空间尺寸会被限制到最大范围内；时间深度也按同样比例采样，使得三维体在视觉上更均衡。

### 4. 三维变换

$$
M=T\cdot R_z\cdot R_y\cdot R_x\cdot S
$$

$$
P_{\text{clip}}=P\cdot V\cdot M\cdot P_{\text{local}}
$$

含义：

时间体像一个三维物体一样被平移、旋转、缩放，然后经过摄像机视图矩阵和透视投影矩阵显示到屏幕上。

### 5. 焦段到视场角

$$
\mathrm{FOV}_y=2\arctan\left(\frac{h_s}{2f}\right)
$$

含义：

`f` 是焦段，`h_s` 是传感器高度。焦段越长，视场角越小，画面越“压缩”；焦段越短，透视越夸张。

### 6. 射线方向

$$
C_l=M^{-1}C_w
$$

$$
\vec d=\frac{P_l-C_l}{\|P_l-C_l\|}
$$

含义：

渲染体积时，程序从摄像机向时间体内部发出射线，沿射线采样颜色和透明度。

### 7. 体渲染 Alpha 累积

$$
\alpha_i=\mathrm{clamp}(\alpha_s\cdot density\cdot \Delta t\cdot 8,0,1)
$$

$$
C_{acc}=C_{acc}+C_i\alpha_i(1-A_{acc})
$$

$$
A_{acc}=A_{acc}+\alpha_i(1-A_{acc})
$$

含义：

每一次采样都像在透明介质里加一层颜色。越往后，剩余可见度越低，所以乘以 `(1-A_acc)`。

### 8. 任意参考面切片

$$
R=R_z(roll)\cdot R_x(pitch)\cdot R_y(yaw)
$$

$$
\vec u=R(1,0,0),\quad \vec v=R(0,1,0),\quad \vec n=R(0,0,1)
$$

$$
p=\vec u f_u+\vec v f_v+\vec n d
$$

含义：

用户旋转参考面，本质上是在定义一个新的局部坐标系。输出图像上的每个像素都会映射回三维时间体里的一个点。

### 9. 三线性插值

$$
C=lerp(lerp(lerp(C_{000},C_{100},f_x),lerp(C_{010},C_{110},f_x),f_y),\dots,f_t)
$$

简化解释：

在 `x,y,t` 三个方向上分别插值，从 8 个邻近体素得到一个平滑采样值。

### 10. 关键帧插值

$$
t=\frac{f-f_0}{f_1-f_0}
$$

$$
v=v_0+(v_1-v_0)t
$$

缓入缓出：

$$
t'=t^2(3-2t)
$$

含义：

关键帧不是只记录结果，而是定义一个随时间变化的函数。

## 成片文稿

### 片头：普通视频其实是一个三维函数

画面建议：

先播放一段普通视频，再切到 ChronoVolume 的 3D 时间体视图。画面中出现一帧帧图像沿时间方向堆叠的示意。

旁白：

我们平时看到的视频，是一张张二维图像按照时间顺序播放。但如果换一个角度看，它其实可以写成一个三维函数：

$$
V(x,y,t)=(R,G,B,A)
$$

这里的 `x` 和 `y` 是画面里的位置，`t` 是时间。每一个点都保存着颜色和透明度。ChronoVolume 做的第一件事，就是把视频从“播放的一串帧”，变成一个可以被旋转、切开、观察的三维时间体。

### 第一段：把帧堆成体

画面建议：

展示导入视频、识别分辨率/FPS/帧数，然后展示普通帧逐渐堆叠成一个立方体或长方体。可以切到程序里的素材信息面板和 3D 预览。

旁白：

在内存里，这个时间体并不神秘。它仍然是一个线性数组，只是我们用三维坐标去理解它：

$$
\text{index}(x,y,t)=((tH+y)W+x)\times4
$$

`W` 是宽度，`H` 是高度，`t` 是第几帧。最后乘以 4，是因为每个点都有 RGBA 四个通道。也就是说，视频的每一帧是一个 `X-Y` 截面，而时间方向就是第三个轴。

为了保证预览流畅，程序会对视频做缩放：

$$
s=\min\left(1,\frac{W_{\max}}{W},\frac{H_{\max}}{H}\right)
$$

然后得到新的宽高：

$$
W'=\mathrm{round}(Ws),\quad H'=\mathrm{round}(Hs)
$$

时间深度也会按比例采样：

$$
D'=\min\left(F,\mathrm{round}(F\cdot s)\right)
$$

这样做的意义是：画面尺寸变小后，时间方向也不再无限拉长，得到的三维体更适合实时观察。

### 第二段：时间体像三维物体一样被渲染

画面建议：

展示用户旋转 3D 视图、改变摄像机、缩放体、切换 Alpha 体/像素体。公式 `P_clip = PVM P_local` 以叠字出现。

旁白：

当视频变成体数据之后，ChronoVolume 就可以像渲染一个三维物体一样渲染它。每个图层都有自己的模型矩阵：

$$
M=T\cdot R_z\cdot R_y\cdot R_x\cdot S
$$

它表示这个时间体在三维世界里的位置、旋转和大小。最后，一个点从局部空间进入屏幕，要经过模型矩阵、摄像机矩阵和投影矩阵：

$$
P_{\text{clip}}=P\cdot V\cdot M\cdot P_{\text{local}}
$$

这也是为什么我们可以给视频体加摄像机、焦段、旋转和缩放。它已经不再只是一个播放器里的画面，而是一个真正的三维对象。

摄像机的焦段会转换成视场角：

$$
\mathrm{FOV}_y=2\arctan\left(\frac{h_s}{2f}\right)
$$

焦段越长，视场角越窄；焦段越短，透视越明显。这让时间体的观察也拥有类似摄影机的语言。

### 第三段：体渲染，其实是在沿射线积分

画面建议：

展示一个半透明时间体，叠加一条从摄像机穿过体的射线动画。然后显示体渲染累积公式。

旁白：

真正有意思的是体渲染。程序不是只画表面，而是从摄像机向体内部发出射线，沿路不断采样颜色和透明度。

摄像机位置先被变换到体的局部空间：

$$
C_l=M^{-1}C_w
$$

射线方向是：

$$
\vec d=\frac{P_l-C_l}{\|P_l-C_l\|}
$$

然后沿着这条射线一步步采样。每个采样点都有一个颜色 `C_i` 和透明度 `alpha_i`：

$$
\alpha_i=\mathrm{clamp}(\alpha_s\cdot density\cdot \Delta t\cdot 8,0,1)
$$

颜色的累积方式是前向 Alpha 合成：

$$
C_{acc}=C_{acc}+C_i\alpha_i(1-A_{acc})
$$

$$
A_{acc}=A_{acc}+\alpha_i(1-A_{acc})
$$

这两个公式的意思是：越靠前、越不透明的内容，对最后画面的影响越大；当累计透明度接近 1 时，后面的内容就几乎看不见了。

所以，ChronoVolume 看到的不是某一帧，而是整个时间体在视线方向上的“积分结果”。

### 第四段：切片，把时间剖开

画面建议：

展示 T 轴切片、X 轴切片、Y 轴切片。然后展示参考面旋转，切片图像随角度改变。公式 `p = u fu + v fv + n d` 出现。

旁白：

如果说 3D 渲染是在看整个时间体，那么切片就是把时间体剖开。

最简单的是轴切片。固定 `T`，看到的是普通视频帧；固定 `X`，看到的是某一列像素随时间的变化；固定 `Y`，看到的是某一行像素随时间的变化。

更自由的是参考面切片。用户通过 yaw、pitch、roll 定义一个旋转矩阵：

$$
R=R_z(roll)\cdot R_x(pitch)\cdot R_y(yaw)
$$

再得到平面上的两个方向和法线方向：

$$
\vec u=R(1,0,0),\quad \vec v=R(0,1,0),\quad \vec n=R(0,0,1)
$$

输出图像上的每个像素，都会被映射回时间体中的一个三维点：

$$
p=\vec u f_u+\vec v f_v+\vec n d
$$

这个公式就是“任意角度切开视频”的核心。你看到的每一张切片，都是这个三维函数在某个平面上的截面。

为了让切片更平滑，程序会使用三线性插值。它不是只取最近的一个体素，而是从周围 8 个体素混合出当前采样值。

### 第五段：关键帧和表达式，把动画变成函数

画面建议：

展示时间线、关键帧、曲线视图、摄像机运动。叠加线性插值和 smoothstep 公式。

旁白：

ChronoVolume 的合成系统里，图层和摄像机都可以打关键帧。数学上，关键帧就是把一个属性变成时间函数。

最基本的线性插值是：

$$
t=\frac{f-f_0}{f_1-f_0}
$$

$$
v=v_0+(v_1-v_0)t
$$

如果要让运动更自然，就使用缓入缓出：

$$
t'=t^2(3-2t)
$$

它让变化在开始和结束时更慢，中间更快。

程序还支持表达式，比如用：

$$
value + 20\sin(time)
$$

让一个属性周期性摆动。这样，动画就不只是手动设置的关键帧，也可以是由公式生成的运动。

### 第六段：合成与遮罩，把多个时间体叠加

画面建议：

展示多个视频体图层叠在一起、混合模式切换、Alpha 轨道遮罩。出现正常 Alpha 合成公式。

旁白：

在合成工作区里，每个素材都可以成为一个时间体图层。多个图层按照时间线顺序渲染，并通过混合模式叠加。

正常模式的 Alpha 合成公式是：

$$
C_{out}=C_s+C_d(1-A_s)
$$

$$
A_{out}=A_s+A_d(1-A_s)
$$

这里 `s` 表示当前图层，`d` 表示下面已经渲染好的内容。程序还支持相加、屏幕、正片叠底和轨道遮罩。轨道遮罩的本质是用一个时间体的 Alpha 去裁切另一个时间体：

$$
C'=Cm,\quad A'=Am
$$

其中 `m` 是遮罩体沿射线采样得到的透明度。

### 第七段：导出与分布式，让数学结果变成视频

画面建议：

展示导出设置、高精度导出、分布式 Worker 面板、进度条、输出视频。出现任务拆分公式。

旁白：

最后，这些数学运算要变成真正的视频文件。普通导出可以直接使用 GPU 切片；高精度导出会回到源视频尺寸，使用 raw cache 重新采样，避免只依赖低分辨率预览体。

对于特别大的任务，ChronoVolume 可以把输出帧范围拆给多台机器：

$$
[0,N-1]\rightarrow [a_0,b_0],[a_1,b_1],...,[a_k,b_k]
$$

每台 Worker 只负责其中一段，完成后再把结果片段拼接起来。这让任意角度、高分辨率、长时间的视频切片也可以被实际导出。

### 结尾：视频不只是播放，也可以被计算

画面建议：

快速回放：导入视频、3D 时间体、参考面切片、合成、导出结果。最后停在一个漂亮的时间体或切片画面。

旁白：

ChronoVolume 想做的事情，是把视频从“只能播放的画面”变成“可以计算的时空结构”。

当我们把视频写成：

$$
V(x,y,t)=(R,G,B,A)
$$

时间就不再只是播放器里的进度条。它可以成为一个方向、一条轨迹、一张切片，甚至一个可以被渲染和合成的三维对象。

这就是 ChronoVolume 背后的数学：把时间变成空间，把影像变成几何，把观看变成探索。

## 可选短版旁白

如果想做 60 到 90 秒短视频，可以使用下面这一版：

普通视频是一串二维图像，但 ChronoVolume 把它看成一个三维函数：

$$
V(x,y,t)=(R,G,B,A)
$$

`x,y` 是画面位置，`t` 是时间。把每一帧沿时间方向堆叠起来，就得到一个三维时间体。

在这个时间体里，普通视频帧只是固定 `t` 的切片；固定 `x` 或 `y`，就能看到某一列或某一行像素随时间变化的轨迹。进一步，我们还可以定义任意参考面：

$$
p=\vec u f_u+\vec v f_v+\vec n d
$$

然后从任意角度切开视频。

3D 预览时，程序会从摄像机向体内部发射射线，沿路采样颜色和透明度：

$$
C_{acc}=C_{acc}+C_i\alpha_i(1-A_{acc})
$$

$$
A_{acc}=A_{acc}+\alpha_i(1-A_{acc})
$$

所以我们看到的不是某一帧，而是整个时间体沿视线方向的累积结果。

图层、摄像机和关键帧又把这些体变成可动画的对象：

$$
v=v_0+(v_1-v_0)t
$$

最终，ChronoVolume 让视频不只是播放，而是可以被旋转、切片、计算、合成和导出的时空几何。
